import React from "react";
import Box from "@mui/material/Box";
import { LineChartAnalysis } from "~/app/_components/chart/LineChartAnalysis";
const test = async () => {
  return (
    <Box sx={{ alignContent: "center", width: "100%" }}>
      <LineChartAnalysis/>
    </Box>
  );
};

export default test;
