import React from "react";
import Box from "@mui/material/Box";
const test = async () => {
  return (
    <Box sx={{ alignContent: "center", width: "100%" }}>
    </Box>
  );
};

export default test;
